import { Card, Flex } from "@radix-ui/themes";
import styled from "styled-components";

export const List = styled(Flex)`
    padding: 10px;
`;

export const PersonListContainer = styled(Card)`
    flex: 1;
`;